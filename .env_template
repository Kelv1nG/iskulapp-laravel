# ==================== postgres credentials ================================
PG_DATABASE_NAME=school_erp
PG_DATABASE_PORT=5432
PG_DATABASE_USER=admin
PG_DATABASE_PASSWORD=admin
PS_DATA_SOURCE_URI=postgres://${PG_DATABASE_USER}:${PG_DATABASE_PASSWORD}@pg-db:${PG_DATABASE_PORT}/${PG_DATABASE_NAME}

# ==================== config =============================================
SCHOOL_ERP_BACKEND_PORT=6061
PS_JWKS_URL=http://school-erp-backend:${SCHOOL_ERP_BACKEND_PORT}/api/get_keys

# These can be generated by following the instructions in the `key-generator` folder
JWKS_PUBLIC_KEY=
JWKS_PRIVATE_KEY=

# ==================== powersync ====================
# The PowerSync API is accessible via this port
PS_PORT=8080

# ==================== pgadmin ======================
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT=5050

# ==================== JWKS KEYS ====================

kty=
n=
e=
alg=
kid=
